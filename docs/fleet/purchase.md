# Fleet Purchase Workflow

Use the fleet namespace command:

```bash
maestro fleet purchase
```

This provisions a project maestro node and updates fleet registry state.

Legacy alias retained with warning:

```bash
maestro-purchase
```
